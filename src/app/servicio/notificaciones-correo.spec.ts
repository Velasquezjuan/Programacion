import { NotificacionesCorreo } from './notificaciones-correo';

describe('NotificacionesCorreo', () => {
  it('should create an instance', () => {
    expect(new NotificacionesCorreo()).toBeTruthy();
  });
});
