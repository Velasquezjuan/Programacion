import { RecuContraServicio } from './recu-contra-servicio';

describe('RecuContraServicio', () => {
  it('should create an instance', () => {
    expect(new RecuContraServicio()).toBeTruthy();
  });
});
